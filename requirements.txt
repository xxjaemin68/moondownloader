aiohttp>=3.14.1
playwright>=1.61.0
pillow>=10.0  # optional — used for custom logo scaling

# Core dependencies
aiohttp>=3.9,<4.0
playwright>=1.40,<2.0

# Optional — used for custom logo scaling in the GUI
pillow>=10.0,<12.0
